# 路线图

这里只记录尚未实现、且已有明确用户价值的候选项；已完成内容归档在
[CHANGELOG.md](CHANGELOG.md)。路线图不是承诺。

## 优先候选

- **重复投稿检测**：以 Telegram `file_unique_id` 为主，文档用文件名与大小辅助判断。
- **定时发布**：在发布预览中选择时间，任务与投稿数据持久化，重启后仍能执行。
- **Owner 周报与备份**：统计摘要和 SQLite 一致性备份，仅发送给 `OWNER_ID`。

## 次级候选

- `/random` 随机推荐历史投稿。
- 讨论组互动数据接入热度模型。
- 多语言文案。

## 当前不做

- Web 管理面板：Bot 内管理已覆盖核心操作。
- 更换数据库：当前规模下 SQLite + WAL 足够。
- 为单一部署方式再造一套配置系统：环境变量、`config.ini` 与运行时策略已覆盖。
