# TelePost 功能路线图（提案）

> 状态：提案，未实现。优先级基于现有代码中已经存在的"半成品钩子"与真实需求缺口评估，实现成本为估算值（S=半天内，M=1-3 天，L=更长）。

## P0 —— 完成已有半成品（性价比最高）

### 1. ~~发布前编辑流程~~ ✅ 已实现（2026-08）
- **实现**：剧透确认后进入发布预览页（`handlers/preview_handlers.py`），提供 ✅确认发布 / 🏷️改标签 / 📝改简介 / 📎补充媒体 / ❌取消 五个按钮；标签与简介为单字段快速覆盖，媒体直接追加（上限 50），每步修改后自动刷新预览。新增 EDIT_TAG/EDIT_NOTE/EDIT_MEDIA 三个会话状态。

### 2. ~~结果列表真实分页~~ ✅ 已实现（2026-08，覆盖 /search 与 /hot）
- **实现**：/search 结果页与 /hot 排行页底部出现 ⬅️/页码/➡️ 导航（`Keyboards.page_nav`），查询上下文存于 `context.user_data['pg']`，page_N 回调重渲染。/myposts 因逐帖独立消息的展示形态暂不支持，保持后续提案。

### 3. ~~投稿速率限制（反滥用）~~ ✅ 已实现（2026-08）
- **实现**：`SUBMIT_LIMIT_PER_HOUR` 配置（默认 10，0 关闭），在 `/submit` 入口做每用户滑动窗口计数（内存态），OWNER/ADMIN 暂不豁免（可在后续按需放开）。

## P1 —— 高价值新功能

### 4. ~~可选投稿审核队列~~ ✅ 已实现（2026-08）
- `API_REVIEW_REQUIRED` 与 `CHAT_REVIEW_REQUIRED` 分别控制 HTTP API 和 Telegram `/submit` 投稿是否先进入私有 `REVIEW_CHAT_ID`，管理员通过按钮发布或拒绝。
- 审核队列保存 Telegram `file_id` 和 SQLite 状态，支持幂等键和防重复审批。
- 两个开关相互独立且默认关闭，可只审核一种投稿来源，也可同时审核。

### 5. 定时发布
- **方案**：发布确认页增加 🕐定时 选项（如 +1h/+3h/明早9点），用 PTB 自带 `job_queue.run_once`（项目已依赖 job-queue extra）在到点执行发布；到期前可取消。
- **成本**：M。

### 6. 重复投稿检测
- **方案**：收集媒体 `file_unique_id`（现有代码只存 `file_id`，需在 `handle_media` 追加一列或 JSON 里带出），入库前对 `published_posts.file_ids` 查重并提示"该媒体已于 YYYY-MM-DD 发布"。纯文档投稿可用文件名+大小近似查重。
- **成本**：M。**价值**：频道运营最常见的痛点。

### 7. OWNER 数据报备与备份
- **方案**：每周 job_queue 汇总（新增帖数/Top5 热门/黑名单变动）私聊推送；`/backup` 命令把 `data/submissions.db` + 搜索索引打包发送给 OWNER（注意 WAL 文件需先 checkpoint）。
- **成本**：S-M。

## P2 —— 锦上添花

- **匿名投稿开关** ✅ 已实现（2026-08）：发布预览页一键切换，按次生效。
- **`/random` 随机回魂**：随机抽取一篇历史热帖重新置顶推荐。
- **讨论群联动**：若频道绑定讨论组，把评论数/回应纳入热度算法（`heat_calculator` 已留扩展位）。
- **多语言（i18n）**：文案集中在 `ui/messages.py`，抽取为语言包成本可控。
- **索引健康自检**：每日 job_queue 跑 `index_manager.status`，异常时私聊 OWNER（现仅在启动时检查）。

## 不建议现在做

- Web 管理面板：收益/成本比低，Bot 内管理命令已覆盖核心操作。
- 更换数据库引擎：SQLite + WAL 对当前量级完全够用（项目文档亦以低资源部署为卖点）。

---
最后更新：2026-08
